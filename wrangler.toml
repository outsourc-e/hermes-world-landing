name = "hermes-world"
compatibility_date = "2026-05-01"
pages_build_output_dir = "public"

# NOTE: GAME_AUTH_SESSIONS KV binding is configured at the Pages project level
# (Cloudflare dashboard), NOT here. A placeholder id in this file breaks
# `wrangler pages deploy` with 'Invalid KV namespace ID'.
